<div class=m-5>
  <h1>My Next Planned Activities!</h1>
  <% if @bookings.any? %>
    <div class="row mt-5">
      <%= render partial: "booking_card", collection: @bookings, as: :booking %>
    </div>
  <% else %>
    <p>You haven't made any bookings yet.</p>
  <% end %>
</div>
